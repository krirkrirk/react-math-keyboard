type Theme = {
    extraLight: string;
    light: string;
    main: string;
    dark: string;
};
export type KeyboardThemeColor = "grey" | "blue" | "purple" | "orange" | "green" | "pink";
export declare const colors: Record<KeyboardThemeColor, Theme>;
export {};
